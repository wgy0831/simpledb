memdata:
	depress
	update

cache:(just for disk data)
	map the first key(key[0])
	search entrance->memdata search->disk dearch->load

the top structure

target:
Throughput
depth
average finding count

lstm b+ 